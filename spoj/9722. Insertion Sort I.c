#include<stdio.h>

int main()
{
    int test,n,i,j, count;
    int input[100000];
    scanf("%d",&test);
    for(i=0;i<test;i++)
    {
        scanf("%d",&n);
        for(j=0;j<n;j++)
            scanf("%d",&input[i]);
        count = InSort(input,n);
    }
}

int InSort(int input[100000],int n)
{
    int i,j;
    for(i=0;i<)
}
